import { OfCategoryPipe } from './of-category.pipe';

describe('OfCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new OfCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
